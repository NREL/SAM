equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ elecload' } = define() {
	/* need to implement */
	return 0; };

