
on_load{'Test 1'} = define() {
	log('script callback for ->on_load - works!');
};

on_change{'chk_hello_1'}= define() {
	msgbox('hello checkbox world!');
};


on_change{'chk_hello_1'} = define() {
	msgbox('basic callback!');
};

on_change{'object 1'} = define() {
	msgbox('basic callback, ready to change input_1');
	value( 'input_1', 3.45 );
};

