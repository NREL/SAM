equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

equations{ '$MIMO$ climate_info' } = define() {
	/* need to implement */
	return 0; };

