1)
Add design parameters to specify independently both high and low side pressure drop for recuperators and the PHX CO2 side pressure drop. Any parameter not defined will default to the input parameter "deltaP_counterHX_frac". If "deltaP_counterHX_frac" is also undefined, the model neglects pressure drops for that flow path.

LTR_LP_deltaP_des_in
LTR_HP_deltaP_des_in
HTR_LP_deltaP_des_in
HTR_HP_deltaP_des_in
PHX_co2_deltaP_des_in

2)